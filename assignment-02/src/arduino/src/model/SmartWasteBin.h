#ifndef __SMART_WASTE_BIN__
#define __SMART_WASTE_BIN__

#include <Arduino.h>

class SmartWasteBin {

    public: 

    private:

    enum {
        IDLE,
        BIN_OPEN
        // and so on
    } state; 
};

#endif
